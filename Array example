using System;

class Program
{
    static void Main()
    {
        int[] numbers = { 10, 20, 30, 40 };

        foreach (int num in numbers)
        {
            Console.WriteLine(num);
        }
    }
}
